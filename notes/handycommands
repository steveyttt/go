go help fmt
go version
go get -d github.com/GoesToEleven/go-programming
go env

1. Install Go from website - https://golang.org/dl/
2. Setup your go workspace:
    - create a dir called goworkspace
    - create 3 dirs in it (pkg, src, bin)
    - Setup go env variables inside your ~/.bash_profile

###############
PATH="/Users/steventyson/Library/Python/2.7/bin:${PATH}:/Users/steventyson/Documents/goworkspace/bin"
GOPATH="/Users/steventyson/Documents/goworkspace"
GOBIN="/Users/steventyson/Documents/goworkspace/bin"
export PATH
export GOPATH
export GOBIN
##############

go fmt (automatically formats code to the correct structure)
go fmt ./... (format all files in a REPO)

go run main.go (Run file called main.go) (Builds and runs the code)
go build (build the code in a binary) then execute with ./binaryname
go install main.go (convert to a binary and add to default bin)(You need to set GOBIN env var first )
go get ./... (Get all imports from a go file)

//cd into the directory with the tests file then run:
go test
go test -v (if you want verbose info)

//test all code from current dir and down
//lints for style
golint ./...

//tests and tries to find and resolve things that could actually be an error. 
go vet ./...

//run a benchmark test
go test -bench . //tests all functions in a test package
go test -bench bob //tests the bob function in a package

go test -cover //get the test coverage % of your code
go test -coverprofile c.out
go tool cover -html=c.out // gives a nice visual display of what functions have proper test coverage